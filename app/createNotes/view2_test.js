'use strict';

describe('myApp.createNotes module', function() {

  beforeEach(module('myApp.createNotes'));

  describe('createNotes controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var createNotesCtrl = $controller('CreateNoteController');
      expect(createNotesCtrl).toBeDefined();
    }));

  });
});
