'use strict';

//Future versions
/*
angular.module('myApp.notes', [])

.service('noteService', ['$http', function($http) {
  this.getNotes = function() {
    return $http({
      method: 'GET',
      url: 'components/notes/notes.json'
    });
  };
  this.createNote = function(noteObj) {
    return $http({
      method: 'POST',
      url: 'components/notes/notes.json',
      data: noteObj
    });
  };
}]);
*/
